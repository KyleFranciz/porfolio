// component to have the dynamic island navbar
"use client";

export default function DynamicIsland() {
  // might make it a motion div to animate in
  // note: idea make the bar either grow on hover or on click
  return <div></div>;
}
